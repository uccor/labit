/**
 * StudentController
 *
 * @description :: Server-side logic for managing students
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

 module.exports = {
 // 	createNew: function (req) {
 // 		var par = req.params.all();
	// 	console.log('par', par);
 // 		var username = par.username;
 // 		var pass1 = par.password1;
 // 		var pass2 = par.password2;
 // 		var name = par.name;
 // 		var lastName = par.lastName;

 // 		try {
 // 			if (pass1 == pass2 && pass1 != null) {
 // 				console.log ('password are = ');
 // 				var js = {
 // 					"username" : username,
 // 					"password" : pass1,
 // 					"name" : name,
 // 					"lastName" : lastName
 // 				}
 // 				console.log (Student);
 // 				//Student.create (js);
 				
 // 				Student.create(req.params.all());
 // 			}
	// 	}
	// 	catch (err) {console.error('Wrong Parameters', err)}
		
	// }
};

